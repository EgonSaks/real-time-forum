version: "3"

services:
  backend:
    build:
      context: . 
      dockerfile: ./backend/Dockerfile 
    ports:
      - "8081:8081"
    volumes:
      - sqlite-data:/database/sqlite
    environment: 
      - GO_ENV=dev
      - DOMAIN=localhost
      - CORS_DOMAIN=https://localhost:8080

  frontend:
    build:
      context: . 
      dockerfile: ./frontend/Dockerfile 
    ports:
      - "8080:8080"
    environment: 
      - GO_ENV=dev

volumes:
  sqlite-data: 

